{
  "compilerOptions": {
    // 目标语言的版本
    "target": "ES5",
    // 指定生成那个模块系统代码
    "module": "CommonJS",
    // 生成声明文件，开启后会自动生成声明文件
    "declaration": true,
    // 指定生成声明文件存放目录
    "declarationDir": "./dist",
    // 只生成声明文件，而不会生成js文件
    "emitDeclarationOnly": true,
    // 兼容只有umd，cjs方式且没有暴露deault属性的包，添加default属性
    "esModuleInterop": true,
    // 是否强制代码中使用的模块文件名必须和文件系统中的文件名保持大小写一致，
    "forceConsistentCasingInFileNames": true,
    // 是否启用所有严格类型检查选项
    "strict": true,
    // 是否忽略所有的声明文件（*.d.ts）类型检查
    "skipLibCheck": true,
  },
  // 指定要编译文件的白名单
  "include": ["./src"]
}
